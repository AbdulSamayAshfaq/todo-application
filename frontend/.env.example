# ===============================================
# Frontend Environment Variables
# ===============================================
# Copy this file to .env.local and update with your values
# cp .env.example .env.local

# ===============================================
# IMPORTANT FOR VERCEL DEPLOYMENT
# ===============================================
# Add these to your Vercel project settings:
# Settings > Environment Variables

# Backend API URL (Hugging Face Spaces backend)
# This MUST start with NEXT_PUBLIC_ to be exposed to the browser
NEXT_PUBLIC_API_URL=https://abdulsamay-todo-bk.hf.space/api

# AI Agent URL (if using AI chatbot features)
# Optional - chatbot will work in fallback mode without this
NEXT_PUBLIC_AI_AGENT_URL=https://your-ai-agent.hf.space

# ===============================================
# LOCAL DEVELOPMENT (Optional)
# ===============================================
# For local testing, uncomment these:
# NEXT_PUBLIC_API_URL=http://localhost:8000/api
# NEXT_PUBLIC_AI_AGENT_URL=http://localhost:8001

# ===============================================
# NOTES
# ===============================================
# 1. NEXT_PUBLIC_ prefix is REQUIRED for client-side exposure
# 2. URLs should NOT have trailing slashes
# 3. Include /api suffix in NEXT_PUBLIC_API_URL
# 4. After changing env vars, restart dev server:
#    npm run dev
